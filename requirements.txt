# The packages the analysis imports directly, at the versions it was run with.
# Python 3.14
#
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
numpy==2.5.1
pandas==3.0.3
matplotlib==3.11.1

# notebooks: the kernel, the latency picker's click widgets, HTML export
jupyterlab==4.6.1
ipywidgets==8.1.8
ipympl==0.10.0        # the %matplotlib widget backend the pickers need
nbconvert==7.17.1
